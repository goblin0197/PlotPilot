# PlotPilot AI Agent Guidelines

本文档旨在为协助开发 PlotPilot（墨枢）项目的 AI 编程助手（如 Cursor、Windsurf 或其他自主编程 Agent）提供项目上下文与架构规范指引。

## 1. 项目概述

PlotPilot 是一个 AI 驱动的长篇创作平台，主打自动驾驶生成、知识图谱管理与风格分析一体化。

- **核心技术栈**：
  - **后端**：Python 3.9+, FastAPI
  - **前端**：Vue 3, TypeScript, Vite, Naive UI
  - **存储/检索**：SQLite (主数据库), 本地 FAISS (向量检索)
  - **大模型接入**：OpenAI 兼容协议, Anthropic Claude, 火山方舟 Doubao

## 2. 架构设计：DDD 四层架构

后端严格采用领域驱动设计（DDD），AI 在生成或修改后端代码时**必须遵守**各层职责边界，禁止跨层污染：

- **`domain/` (领域层)**：核心业务逻辑、实体 (Entity)、值对象 (Value Object)、仓储接口定义。**禁止**依赖其他外部层。
- **`application/` (应用层)**：应用服务、用例 (Use Cases)、DTO 转换。负责编排领域层对象完成具体业务。
- **`infrastructure/` (基础设施层)**：具体技术实现，如数据库仓储实现、LLM 客户端封装、FAISS 检索服务、外部 API 调用等。
- **`interfaces/` (接口层)**：对外暴露的端点，如 FastAPI 路由 (Routers)、请求/响应 Pydantic 模型、CLI 入口。

## 3. 核心机制与管线

- **统一落库管线**：HTTP 手动触发与后台守护进程自动驾驶，必须共用同一套章后处理管线（摘要提取 → 三元组解析 → 伏笔处理 → SQLite 落库 + FAISS 索引更新），确保逻辑不漂移。
- **守护进程 (Daemon)**：负责按阶段推进（宏观规划 → 幕级节拍 → 章节生成 → 章末审阅）。
- **状态推流**：使用 SSE (Server-Sent Events) 向前端实时推送生成状态与内容。

## 4. 编码规范与约束

1. **Simplicity First (简单优先)**：编写满足当前需求的最小可行代码。不写预测性代码，不引入非必要的抽象层。
2. **Surgical Changes (精准修改)**：只修改与当前任务直接相关的代码，不要“顺手”重构或优化无关的正常运行代码。
3. **类型提示**：Python 后端必须写完整的 Type Hints；前端必须严格使用 TypeScript 类型，尽量避免使用 `any`。
4. **语言要求**：默认使用简体中文编写代码注释和文档；代码标识符、日志键名等保持英文。
5. **保护现有代码**：修改时必须保留原有无关注释，确保原有功能不受破坏。

## 5. 任务执行指南

- **新增业务功能时**，思考顺序应为：`domain` -> `application` -> `infrastructure` -> `interfaces`。
- **排查问题时**，请先定位问题所属的具体架构层级，并留意多路数据（如 Story Bible、向量检索、伏笔台账）在上下文组装时是否存在干扰。
- 如果需求模糊或多种实现路径存在权衡，**必须先停下来向用户确认**，不要盲目猜测。
